# 快速测试 HTML Bundle 上传功能

## 🚀 立即测试（3 分钟）

### 前提条件

✅ 开发服务器已经在运行（Terminal 10）
✅ 示例 HTML Bundle 已创建：`~/Downloads/html-bundle-example.zip`

### 测试步骤

1. **打开浏览器**
   ```
   访问：http://192.168.187.128:3000
   （或你配置的开发服务器地址）
   ```

2. **登录账号**
   - 使用你的测试账号登录

3. **创建新帖子**
   - 点击右下角或顶部的"发帖"按钮

4. **找到 HTML Bundle 按钮**
   - 在发帖工具栏底部找到 📄 图标
   - 位置：在"从云盘选择"(☁️) 和"投票"(📊) 按钮之间

5. **上传示例文件**
   - 点击 📄 图标
   - 选择 `~/Downloads/html-bundle-example.zip`
   - 阅读弹出的说明对话框
   - 点击"确定"确认上传

6. **等待上传**
   - 观察上传进度
   - 上传完成后，附件区域会显示该文件

7. **发布帖子**
   - （可选）输入一些文字，如"测试 HTML Bundle 功能"
   - 点击"发布"按钮

8. **查看效果**
   - 在帖子中会看到一个预览卡片
   - 卡片显示文件名和"点击查看 HTML 内容"提示
   - **点击预览卡片**
   - iframe 会展开，显示交互式内容

9. **测试交互**
   - 看到渐变背景的卡片
   - 点击"点击我！"按钮
   - 观察计数器数字增加
   - 观察缩放动画效果

10. **折叠内容**
    - 点击右上角的 ↑ 按钮
    - iframe 会收起到预览状态

---

## 🎯 预期效果

### 工具栏按钮
```
📷 上传  ☁️ 从云盘  📄 HTML Bundle  📊 投票  👁️ CW  # 话题  @ 提及
```

### 说明对话框
```
标题：上传 HTML Bundle（ZIP 压缩包）

内容：
上传包含 HTML、CSS 和 JavaScript 的 ZIP 压缩包。
ZIP 文件必须在根目录包含 'index.html' 文件。
这将在帖子中显示为一个可交互的组件。

[取消]  [确定]
```

### 展开后的效果
```
╔══════════════════════════════════╗
║  🎉 HTML Bundle 测试              ║
║  这是一个交互式的 HTML Bundle 组件！ ║
║  ┌──────────┐                    ║
║  │ 点击我！  │  ← 可以点击          ║
║  └──────────┘                    ║
║  点击次数: 5  ← 会增加             ║
╚══════════════════════════════════╝
```

---

## 🔍 多语言测试

### 切换到英文界面
1. 点击右上角头像
2. 设置 → 通用 → 语言
3. 选择 English
4. 刷新页面
5. 按钮提示应显示为 "Attach HTML Bundle (ZIP)"

### 切换到日文界面
1. 同样的设置路径
2. 选择 日本語
3. 按钮提示应显示为 "HTML Bundle（ZIP）を添付"

---

## 📝 创建自己的 HTML Bundle 测试

### 最简示例（30 秒）

```bash
# 创建测试文件
mkdir /tmp/quick-test && cd /tmp/quick-test

# 创建 index.html
cat > index.html << 'EOF'
<!DOCTYPE html>
<html>
<head><meta charset="UTF-8"></head>
<body>
    <h1 style="color: blue;">Hello World!</h1>
    <button onclick="alert('It works!')">点我</button>
</body>
</html>
EOF

# 打包
zip quick-test.zip index.html

# 现在可以上传 quick-test.zip 测试
```

### 带样式的示例（1 分钟）

```bash
mkdir /tmp/styled-test && cd /tmp/styled-test

# HTML
cat > index.html << 'EOF'
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="card">
        <h1>🎨 样式测试</h1>
        <p>这是一个带样式的 HTML Bundle</p>
    </div>
</body>
</html>
EOF

# CSS
cat > style.css << 'EOF'
body {
    margin: 0;
    padding: 20px;
    background: linear-gradient(45deg, #ff6b6b, #4ecdc4);
    font-family: sans-serif;
}
.card {
    background: white;
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.3);
}
EOF

# 打包
zip styled-test.zip index.html style.css
```

---

## 🛠️ 故障排查

### 问题：按钮不显示

**可能原因：** 前端代码未重新编译

**解决方法：**
```bash
cd /home/skyler/Documents/Projects/Forsion-Infinity/misskey-develop
pnpm build --filter frontend
```

然后刷新浏览器（Ctrl+Shift+R 强制刷新）

---

### 问题：上传后显示错误

**检查项：**
1. ZIP 文件是否在根目录包含 `index.html`？
2. 文件大小是否超过 10MB？
3. 是否包含禁止的文件类型（.exe, .php 等）？

**验证 ZIP 内容：**
```bash
unzip -l ~/Downloads/html-bundle-example.zip
```

应该看到：
```
Archive:  html-bundle-example.zip
  Length      Date    Time    Name
---------  ---------- -----   ----
      XXX  01-25-2025 00:00   index.html
      XXX  01-25-2025 00:00   style.css
      XXX  01-25-2025 00:00   script.js
```

**重要：** `index.html` 必须在列表的根级别，不能在子文件夹中！

---

### 问题：iframe 显示空白

**可能原因：** HTML 内容有错误

**调试方法：**
1. 按 F12 打开浏览器开发者工具
2. 切换到 Console 标签
3. 查看是否有 JavaScript 错误
4. 切换到 Network 标签
5. 查看资源是否加载成功

---

### 问题：JavaScript 不执行

**可能原因：** 沙箱限制

**解决方法：** 确保使用标准的浏览器 API，避免：
- 弹出窗口（`window.open()`）
- 表单提交到外部
- 访问父页面（`window.parent`）

**允许的操作：**
- ✅ DOM 操作
- ✅ 事件监听
- ✅ 动画
- ✅ 本地存储（localStorage）
- ✅ Fetch API（同源）

---

## 📊 测试检查表

使用这个清单确保所有功能正常：

- [ ] 工具栏显示 HTML Bundle 按钮（📄 图标）
- [ ] 鼠标悬停显示提示文本
- [ ] 点击按钮打开文件选择器
- [ ] 只能选择 .zip 文件
- [ ] 显示说明对话框
- [ ] 对话框文本正确（根据语言）
- [ ] 上传进度正常显示
- [ ] 上传完成后显示在附件列表
- [ ] 发布后显示预览卡片
- [ ] 预览显示文件名
- [ ] 点击预览展开 iframe
- [ ] HTML 内容正确显示
- [ ] JavaScript 可以执行
- [ ] CSS 样式正确应用
- [ ] 点击折叠按钮收起
- [ ] 切换语言后文本更新

---

## 🎉 成功标志

如果你看到以下效果，说明功能完全正常：

1. ✅ 工具栏有 HTML Bundle 按钮
2. ✅ 可以成功上传 ZIP 文件
3. ✅ 帖子中显示交互式内容
4. ✅ 可以点击按钮，计数器增加
5. ✅ 样式和动画正常显示
6. ✅ 多语言切换正常

---

## 📸 预期截图参考

### 1. 工具栏按钮
```
发帖框底部：
[📷] [☁️] [📄] ← 这个！ [📊] [👁️] [#] [@] [🎨]
```

### 2. 说明对话框
```
┌─────────────────────────────────────┐
│ 上传 HTML Bundle（ZIP 压缩包）         │
├─────────────────────────────────────┤
│ 上传包含 HTML、CSS 和 JavaScript 的   │
│ ZIP 压缩包。ZIP 文件必须在根目录包含   │
│ 'index.html' 文件。这将在帖子中显示   │
│ 为一个可交互的组件。                  │
│                                     │
│         [取消]  [确定]               │
└─────────────────────────────────────┘
```

### 3. 展开效果
```
帖子中：
┌────────────────────────────────────┐
│ html-bundle-example.zip         [↑]│
├────────────────────────────────────┤
│                                    │
│     渐变背景（紫色到粉色）            │
│                                    │
│   ┌──────────────────────┐        │
│   │   🎉 HTML Bundle 测试  │        │
│   │  这是一个交互式的...    │        │
│   │   ┌──────────┐        │        │
│   │   │ 点击我！  │         │        │
│   │   └──────────┘        │        │
│   │  点击次数: 3           │        │
│   └──────────────────────┘        │
│                                    │
└────────────────────────────────────┘
```

---

## 🎓 进阶测试建议

完成基础测试后，可以尝试：

1. **图表测试**
   - 使用 Chart.js 创建图表
   - 测试数据可视化效果

2. **游戏测试**
   - 创建简单的猜数字游戏
   - 测试用户交互

3. **动画测试**
   - 使用 CSS 动画
   - 测试性能表现

4. **大文件测试**
   - 创建接近 10MB 的 HTML Bundle
   - 测试上传和显示性能

5. **安全测试**
   - 尝试上传包含危险代码的 HTML
   - 验证沙箱隔离效果

---

## 📞 需要帮助？

如果遇到问题：

1. 查看浏览器控制台错误
2. 查看后端日志（Terminal 10）
3. 检查文档：`Document/Function/HTML-Bundle-上传功能.md`
4. 查看实现总结：`Document/Function/HTML-Bundle-按钮实现总结.md`

---

**祝测试顺利！** 🎉
