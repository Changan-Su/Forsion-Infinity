# Forsion Backend 前端集成配置示例
# 复制此文件为 .env 并修改实际配置

# ------------------------------------------------------------------------------
# Forsion Backend URL 配置
# ------------------------------------------------------------------------------

# Forsion 登录页面 URL
# 用户点击"Login with Forsion"按钮后会重定向到此 URL
# 开发环境示例：
VITE_FORSION_AUTH_URL=http://localhost:3001/auth

# 生产环境示例（取消注释并修改为实际地址）：
# VITE_FORSION_AUTH_URL=https://api.forsion.com/auth

# Forsion Backend API 基础 URL
# 用于 token 验证等 API 调用
# 开发环境示例：
VITE_FORSION_API_URL=http://localhost:3001

# 生产环境示例（取消注释并修改为实际地址）：
# VITE_FORSION_API_URL=https://api.forsion.com

# ------------------------------------------------------------------------------
# 注意事项
# ------------------------------------------------------------------------------

# 1. 修改此文件后需要重启前端开发服务器 (pnpm dev)
# 2. 生产构建时会使用这些环境变量
# 3. 确保 URL 不包含尾部斜杠
# 4. 生产环境必须使用 HTTPS
# 5. VITE_ 前缀的变量会暴露给浏览器，不要放置敏感信息
