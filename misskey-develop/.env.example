# Misskey 环境变量示例
# 复制为 .env 后按需修改。使用：pnpm start:env（会加载 .env 再启动）
# 亦可：set -a && source .env && set +a && pnpm start

# ------------------------------------------------------------------------------
# 端口与配置
# ------------------------------------------------------------------------------

# 监听端口（仅当 default.yml 未设置 port 时生效，否则以 yml 为准）
# PORT=3000

# 配置文件路径（相对 .config/，替代 default.yml）
# MISSKEY_CONFIG_YML=default.yml

# 实例 URL（config 未配置 url 时的回退）
# MISSKEY_URL=https://example.tld/

# ------------------------------------------------------------------------------
# 本地 / 开发
# ------------------------------------------------------------------------------

# 本地开发时 WebFinger 使用 HTTP（勿用于生产）
# MISSKEY_WEBFINGER_USE_HTTP=false

# Node 环境：development | production | test
# NODE_ENV=development

# ------------------------------------------------------------------------------
# 数据库回退（通常用 default.yml / docker.env 即可）
# ------------------------------------------------------------------------------

# DATABASE_DB=misskey
# DATABASE_USER=example-misskey-user
# DATABASE_PASSWORD=example-misskey-pass

# ------------------------------------------------------------------------------
# Forsion Backend 集成（新增）
# ------------------------------------------------------------------------------

# 注意：Forsion 配置主要在 .config/default.yml 中设置
# 前端环境变量在 packages/frontend/.env 中配置
